@page "/auth"

@using Microsoft.AspNetCore.Authorization

@attribute [Authorize]

<PageTitle>Auth</PageTitle>

<h1>@L["Auth_Authenticated_Title"]</h1>

<AuthorizeView>
    @L["Auth_Authenticated_Hello", context.User.Identity?.Name ?? ""]
</AuthorizeView>
